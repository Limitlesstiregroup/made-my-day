{
  "name": "made-my-day",
  "version": "1.0.0",
  "private": true,
  "description": "Made My Day - anonymous positive stories platform",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-check.js",
    "test": "node scripts/smoke-test.js && node scripts/e2e-api.js",
    "lint": "node scripts/lint-check.js",
    "dev": "node server.js",
    "start": "node server.js",
    "release:check": "node scripts/release-readiness.js",
    "ga:quickstart": "node scripts/ga-quickstart.js",
    "ga:gate": "npm run build && npm run lint && npm test && npm run release:check",
    "ga:gate:local": "bash -lc 'npm run build && npm run lint && npm test && set -a; . ./.env.ga.local; set +a; npm run release:check'"
  }
}
